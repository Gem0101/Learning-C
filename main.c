// Access console functions so we can print shit to console:
#include <stdio.h>

// Program entry point returning nothing(void) and taking nothing(void)
// <Return> <function name>(<input>)
void main(void) {
    float dono = 4.6;
    printf("Hello world %f\n", dono);
    return;
}
